# Supabase public browser credentials.
# Copy this file to .env.local and fill these values from the Supabase project dashboard.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Do not store service-role keys or database passwords in client-side env files.
